node_modules/
*.log
.DS_Store

# Plugin build artefacts and local user state
plugin/main.js
plugin/data.json

# Dev vault is per-developer scratch space; not tracked. See README.
dev-vault/

# Claude CLI session store, if it ever leaks into the tree
.claude/projects/
